[project]
name = "slurmviz"
version = "0.1.0"
description = "A terminal UI dashboard for Slurm GPU cluster status"
requires-python = ">=3.10"
dependencies = [
    "textual>=1.0.0",
]

[project.scripts]
slurmviz = "slurmviz:main"
